<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

        //item name="android:windowFullscreen">true/item //Растинуть окно на весь экран.
        //item name="android:windowContentOverlay">@null/item //Информация которая будет перекрывать
        окно со значением ноль(ничто перекрывать окно не
        будет).
        //item name="android:windowNoTitle">true/item //Нез заголовка у окна (значение true).


    <style name="StyleButton" parent="AppTheme">
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/style_btn_press_on</item>
        <item name="android:layout_margin">16dp</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textSize">25sp</item>
    </style>

</resources>